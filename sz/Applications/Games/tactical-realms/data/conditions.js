;(function() {
  'use strict';
  // Conditions are defined inline in condition-engine.js
  // This file exists for the _pending pattern consistency but the conditions
  // are hardcoded in the engine since they need behavioral code, not just data.
  // See condition-engine.js for the full CONDITIONS object.
})();
